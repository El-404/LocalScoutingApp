import QtQuick

Item {
    property String name
    property String label
    property String scoringType
    property int lowRangeValue
    property int highRangeValue
}

//match, team, and scout id  - I don't remember what this comment was about

//add a next button, so that you can easily switch match #'s
//user chooses team to scout per match
//add a match comment feature
//objs and obj ids

//read/write queries at -> /qtUpload.php?scoutid=...
